en:
  errors:
    messages:
      cannot_contain_email: cannot contain an email address
      cannot_contain_url: cannot contain a URL

  attributes:
    email: Email address

  activerecord:
    errors:
      models:
        user:
          attributes:
            password_confirmation:
              confirmation: doesn't match password
